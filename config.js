const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "password",
        database: "database"
    },
    email: {
        mail: "mail",
        password: "password",
        from: "from"
    }
}

module.exports = config;